doctype
html
  head
    include _annoying_headers
    if (title === titleSuffix)
      if public.articles._data[current.path[1]]
        title= public.articles._data[current.path[1]].title + ' | ' + titleSuffix
      else
      title= title
    else
      title= title + ' | ' + titleSuffix
    //- title=title===titleSuffix ? title : title + ' | ' + titleSuffix

    link(rel="stylesheet" href="/assets/main.css")
    script(src="/assets/bundle.js")
    include _analytics
  body.abs
    div.leftbar
      include nav

    div.main
      div.main-content
        if current.path[0] === 'articles'
          block article
        else
          != yield

    div.footer
      //- div.navbar-toggle.footer-float-btn
        span.icon-bar
        span.icon-bar
        span.icon-bar

      div.back-to-top.footer-float-btn ^
      //- <div class="back-to-top fadeout" style="left: 0px; border-radius: 0px 10px 10px 0px; z-index: 2;">^</div>
      div
        a(style="color: #AAA;", title="View Source Code", href="https://github.com/chuyik/chuyik.github.io/tree/harp", target="_blank") IMAO I design it myself.
        div(style="margin:0 0 5px 0")
          small(style="color: #CCC;") Powered by
            a(style="color: #CCC; padding: 0 0 0 3px", href="http://harpjs.com", target="_blank") Harp